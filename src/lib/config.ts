// App-wide configuration constants. No secrets — this is a fully static client app.

/** Address used by the "Report an issue" control (mailto: only — no network call). */
export const REPORT_EMAIL = 'geoff.amess@comotion.co.za'

export const APP_NAME = 'Claude Mastery'
