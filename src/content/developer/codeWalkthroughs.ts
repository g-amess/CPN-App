import type { Walkthrough } from '../types'

/** Walkthroughs authored with Build modules (author-dev-build). */
export const walkthroughs: Walkthrough[] = []

export function walkthroughById(_id: string): Walkthrough | undefined {
  return undefined
}
